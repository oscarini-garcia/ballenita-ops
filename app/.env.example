# Sincronización (Fase 1) — documento compartido por evento.
# De momento la app es local (Fase 0) y estas variables no hacen falta.
# Cuando enchufemos la sync, crea un bin en https://jsonbin.io y copia aquí:
VITE_JSONBIN_ID=
VITE_JSONBIN_KEY=

# Auth por email mágico (Fase 1) — proveedor de correo (Resend, Postmark…).
VITE_EMAIL_FROM=
